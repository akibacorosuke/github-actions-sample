          echo Add other actions to build,
          echo test, and deploy your project.
          echo Is maven installed?
          mvn --version
          echo Is jave installed?
          java -version
          echo Is gradle installed?
          gradle --version
          echo ==Python==
          python --version
          echo ==Node==
          node --version
          ls -a
          echo pwd
          pwd
          echo env
          env
          echo selenium jars
          echo $SELENIUM_JAR_PATH
          echo Android SDK root
          echo $ANDROID_SDK_ROOT
